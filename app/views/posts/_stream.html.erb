<%= render 'shared/search' %>				
<div id="counter"><%= Post.all.to_a.count %> posts (<%= @search_results.to_a.count %> matches)</div>
<% if @posts.any? %>
<%= render 'shared/paginate', size: size %>	
	<div class="posts">		
		<%= render partial: 'stream_item', collection: @posts %>		
	</div>	
<% else %>
	no posts at this time
<% end %>