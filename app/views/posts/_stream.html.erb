<%= render 'shared/search' %>
<% if @posts.any? %>	
	<div class="posts">	
	<%= will_paginate @posts %>		
		<%= render partial: 'stream_item', collection: @posts %>
	</div>	
<% else %>
	no posts at this time
<% end %>