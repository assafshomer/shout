<%= form_for(@post) do |f| %>			
	<%= render 'shared/errors', object: f.object %>	
	<div class="field">
		<%= f.text_area :content, placeholder: post_place_holder %>
	</div>				
	<div class="field pad-left">
		<%= f.submit submit_button_title,  id: "shoutup_button",
																		class: "btn btn-large btn-primary"  %>	
	</div>

																
<% end %>		